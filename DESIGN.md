# Webポートフォリオサイト構築 基本設計書 

 

## 1. システム概要・目的 

本プロジェクトは、静的サイトジェネレーター（Hugo）およびクラウドサービス（GitHub Pages / GitHub Actions）を活用し、完全無料枠かつ運用負荷を極力抑えた個人技術ブログ／ポートフォリオサイトを構築することを目的とする。 

 

### 1.1 導入背景およびコンテンツ方針 

* **導入背景:** 動的CMS（WordPress等）と比較し、データベース管理が不要でセキュリティリスクが低く、表示速度が高速である静的サイト構成を採用する。 

* **コンテンツ運用方針:** IT技術の学習ログ（コマンド・インフラ構築ナレッジ）および日次の活動記録（トレーニングログ等）を継続的に発信・運用する。 

 

--- 

 

## 2. システム構成・アーキテクチャ 

 

### 2.1 全体構成図（概念） 

[ ローカル開発環境 (Windows11 / WSL2 Ubuntu) ] 

      │ 

      │ 1. Markdown作成 (`content/posts/`) / カスタムCSS編集 / `hugo server` 検証 

      ▼ 

[ リモートリポジトリ (GitHub: main branch) ] 

      │ 

      │ 2. git push（自動検知トリガー） 

      ▼ 

[ CI/CD パイプライン (GitHub Actions: hugo.yml) ] 

      │ 

      │ 3. peaceiris/actions-hugo による自動ビルド・配信生成 

      ▼ 

[ 本番公開環境 (GitHub Pages) ] 

      │ 

      ├─► トップページ: https://kino-r9321.github.io/my_website/ 

      └─► 記事・トラブルシューティング一覧: https://kino-r9321.github.io/my_website/posts/ 

 

### 2.2 採用技術スタック（Technology Stack） 

| 分類 | 採用技術 / ツール | 備考 / 用途 | 

| :--- | :--- | :--- | 

| **開発OS環境** | Windows 11 / WSL2 (Ubuntu) | Linuxコマンド操作およびHugo実行環境 | 

| **静的サイトジェネレーター** | Hugo | Go製高速静的サイト生成 | 

| **サイトテーマ** | PaperMod | レスポンシブ対応、ダークモード、SEO最適化済テーマ | 

| **バージョン管理** | Git / GitHub | ソースコードおよび設定ファイルの管理 | 

| **CI/CD・デプロイ** | GitHub Actions / GitHub Pages | ワークフローによる自動ビルドおよびホスティング | 

| **カスタムデザイン** | CSS | `assets/css/extended/custom.css` | 

 

--- 

 

## 3. 非機能要件・設計方針 

 

1. **コスト最適化:** 初期費用および月額ランニングコストを完全に 0 円に抑える。 

2. **保守性・再現性:** ソースコード・設定ファイルはすべて Git で管理し、手順書を参照することで別端末でも同様の環境が即座に復元できる状態にする。 

3. **セキュリティ:** 静的コンテンツのみの配布とし、公開領域にデータベースやサーバーサイド処理を持たせない構成とする。 

 

--- 

 

## 4. 障害対応・トラブルシューティング実績（構築・運用ナレッジ） 

 

| No | 発生現象 | 原因分析 | 対処方法・恒久対策 | 

| :-: | :--- | :--- | :--- | 

| **1** | **WSL起動時にWindows領域（`/mnt/c/Users/...`）に迷い込む** | WSL起動時のカレントディレクトリがLinuxのホーム領域（`~`）ではなく、Windowsのユーザー領域になっていたため。 | `cd` コマンドを単体で実行し、Linuxホーム領域へ戻った。 | 

| **2** | **Hugoで新規記事がブラウザに表示されない** | Hugoの仕様上、記事ファイルの先頭にある `draft: true`（下書き状態）が有効だと通常ビルドで除外されるため。 | `draft: false` に変更するか、確認時に `hugo server -D` オプションを付けて実行した。 | 

| **3** | **ディレクトリ指定エラー（No such file or directory）** | 意図したフォルダ（`my_website` 等）と異なる場所から相対パスでコマンドを実行したため。 | `pwd` で現在地を確認し、正しいディレクトリへ `cd` で移動してから操作を行った。 | 

| **4** | **"localhost:1313" に接続できない** | WSL側でローカル開発用サーバーが起動していないため（WSLで `hugo server` を起動するコマンドを打ち込んでいないため）。 | ターミナルで `hugo server -D` を入力・実行し、ブラウザ（Edge等）で閲覧可能になった。 | 

| **5** | **CI/CD導入時のコマンド入力の際のエラー** | Word等からWSL（ターミナル）にコピペした際、コマンドの末尾（各行）に全角スペースが含まれていたため。 | 全角スペースを削除（半角スペースなら問題なし）。 | 

| **6** | **GitHubへのアップロードコマンド時のエラー** | Personal Access Token に `workflow` 権限がないため（YAMLファイルの編集をアップロードするのに権限不足でエラーが発生）。 | GitHub内でトークンの権限設定を変更（`repo` だけでなく `workflow` にもチェックを入れる）し、再度 `git push origin main` を実行。 | 

| **7** | **'Could not resolve host: github.com' エラー表示** | PCおよびWSLが一時的にネットワーク接続（Wi-Fi）から切れていたため。 | Wi-Fiに再接続し、再度実行（ユーザーネーム等の確認を経て完了）。 | 

| **8** | **CSSデザインが反映されない** | 使用しているテーマ（PaperMod）に合わない設定・ファイル配置をしていたため。 | `hugo.toml` にPaperMod用の設定を追加し、カスタムCSSの配置場所（`assets/css/extended/`）やセレクタ構造を修正した。 | 

| **9** | **GitHub Actionsのビルド失敗（赤×マーク表示）** | `.github/workflows/hugo.yml` 内のHugoインストール用URL等に不要なスペースがあったため。 | `workflows/hugo.yml` 内のコマンドを公式の動作保証されたアクション（シンプルな設定）に書き換えてPushした。 | 

| **10** | **本番サイト（スマホ版）での表示・リンクエラー** | `hugo.toml` 内の `baseURL` が初期設定（`example.org`）のままで、参照崩れや `Example Domain` への遷移が発生していたため。 | `baseURL` を本番用の絶対URLに変更してPushした。 | 
